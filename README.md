# Validated Test Data
> Современная Java-библиотека для генерации *гарантированно валидных* тестовых данных с использованием Java 17+ и принципов чистой архитектуры.

[![Java](https://img.shields.io/badge/Java-17+-blue?logo=openjdk)](https://openjdk.org/)
[![Лицензия](https://img.shields.io/badge/Лицензия-Apache_2.0-green.svg)](https://opensource.org/licenses/Apache-2.0)
[![Статус сборки](https://github.com/victoriasemkina/validated-test-data/actions/workflows/maven.yml/badge.svg)](https://github.com/victoriasemkina/validated-test-data/actions)

## 🎯 Зачем эта библиотека? (Это не просто ещё один Faker)

Да, такие библиотеки, как **DataFaker**, отлично генерируют *случайные строки*. Но тестовые данные для сложных доменов — это не просто случайные строки. Это **семантически корректные, взаимосвязанные объекты, которые должны удовлетворять вашим бизнес-правилам и ограничениям валидации**.

Эта библиотека работает **поверх** DataFaker и решает её ключевые недостатки:

| Проблема с обычными Faker'ами | Решение с Validated Test Data |
| :--- | :--- |
| ❌ **Нет контекста:** `firstName` и `lastName` генерируются независимо. А как насчёт `email`, который должен логически состоять из них? | ✅ **Контекстная генерация:** Определяйте связи между полями (напр., `email = firstName.lastName@company.com`). |
| ❌ **Нет гарантии валидности:** Вы получаете строку, *похожую* на email. Но пройдёт ли она вашу специфичную валидацию `@Email(regexp = ".+@company\\.com")`? | ✅ **Метод `.buildValid()`:** Главное обещание. Генерирует объект, **гарантированно проходящий Jakarta Bean Validation**. |
| ❌ **Нет бизнес-логики:** Как сгенерировать `Order`, где `totalAmount` должен равняться сумме `itemPrices`? Приходится писать glue-код. | ✅ **Встроенная поддержка правил:** Определяйте простые бизнес-правила через Fluent API. |
| ❌ **Сложно расширить** под кастомные типы данных вашего домена. | ✅ **Чистая, расширяемая архитектура** (Ports & Adapters) позволяет легко добавлять свои генераторы. |

## ✨ Возможности

*   **Гарантия `buildValid()`:** Главный метод. Возвращает экземпляр, гарантированно удовлетворяющий всем ограничениям Bean Validation (`@NotNull`, `@Email`, `@Size`, `@Pattern`).
*   **Fluent API:** Интуитивный построитель для кастомизации генерации (`override()`, `withRule()`).
*   **Современный Java:** Построена на **Java 17+, Records, Sealed Classes** для типобезопасности и чистого кода.
*   **Готова к интеграции:** Модульная архитектура. Использует DataFaker внутри как источник данных.
*   **Лёгкая:** Нулевые зависимости, кроме Jakarta Validation API и DataFaker.

## 🚀 Быстрый старт

### 1. Подключение (Maven)
*Библиотека находится в активной разработке. Пока можно склонировать и собрать локально.*
```xml
<!-- Скоро будет в Maven Central -->
<dependency>
    <groupId>io.github.victoriasemkina</groupId>
    <artifactId>validated-test-data</artifactId>
    <version>1.0.0</version>
</dependency>
